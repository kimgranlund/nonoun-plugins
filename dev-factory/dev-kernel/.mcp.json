{
  "mcpServers": {
    "factory-query": {
      "command": "python3",
      "args": ["${CLAUDE_PLUGIN_ROOT}/bin/factory-query-mcp.py"],
      "env": {
        "DEV_FACTORY_DIR": "${user_config.factory_dir}"
      }
    }
  },
  "userConfig": {
    "factory_dir": {
      "title": "dev-factory state directory",
      "type": "directory",
      "description": "Read-only path to the project's dev-factory state directory (.agents/dev-factory) — the lattice.json, index.db, ledger/, and signals/ the read perimeter surfaces. The dev-server scaffolds it on boot. Optional — leave unset and the read tools return a clear configure-factory_dir message.",
      "required": false
    }
  }
}
