# Changelog

## 0.2.52 — 2026-06-16

- **carve-quality's first empirical application — `empirical_applications` 0 → 1; the score-record writer is now rubric-aware.** PLAN "Next" #3's open half: the `carve-quality` rubric had never been applied to a real carve. Scored one — a cold carve of **product-forge** (8 skills), graph mapped by the `carve-analyst` worker, recorded at `reviews/2026-06-16-product-forge-carve.md` + `scores/product-forge-carve.json`. **Verdict BLOCKED — do not carve:** the analysis is exemplary (D1 graph-fidelity 5, D5 node-accounting 5, D7 buildable 4) but the best-available partition fails the joint test (D2 cuts-at-joints 2, D6 granularity 2) — product-forge is a cohesive hub-and-spoke with a *checked* maker→judge spine (`bin/check-methods.py`), so every boundary either severs a dense cluster or yields plugins that can't stand alone. The gates (D3 no `../`, D4 acyclic, D5 complete) confirm the analysis is *sound* even though the recommendation is "don't" — the rubric cleanly separated analysis quality from partition quality. **Gap surfaced + fixed (the application's payoff):** `score-record.py` was holistic-only (its `CANON` hardcoded P1–P9), so a carve-quality D1–D7 record could neither be written nor pass CI validation. The writer is now **rubric-aware** — dimension vocabularies keyed by rubric family (`plugins-holistic` P1–P9, `carve-quality` D1–D7), each record's dimensions validated against the rubric it adopts (a holistic dim under carve-quality, or vice versa, is rejected by construction); selftest extended; existing holistic records still validate. plugin.json 0.2.51 → 0.2.52.

## 0.2.51 — 2026-06-16

- **check-recall — the I-13 deeper half COMPLETE: all 18 council/skill recall corpora now re-sourced from real transcripts.** The 0.2.44 flagship fix re-sourced one corpus (`plugins-factory-check.recall.json`); the other 17 were still author-paraphrases — a closed loop (patterns matching the author's paraphrase of the patterns). All are now lifted (lightly trimmed, `_provenance`-marked) from the committed council/skill `runs/*.md`: brand-forge (4 — Northwind/Lumina/Verve/Halcyon), product-forge (9 sub-councils — Atlas/Pulse/Aura/Sentry/Beacon/QuickCart/Nimbus/Flow/Concierge), agent-ops (3 — Nightshift/OmniDesk + the repo-review seeded-repo baseline), and plugins-factory `check-hollow` (docs-studio). A green `check-recall` is now a *measurement* across the whole suite (the patterns still catch wordings the council really produced), not a tautology. **Two genuine brittle patterns surfaced and were fixed** (the point of the exercise): `brand-forge/check.py` D2 missed the council's "every premium coffee brand could publish this verbatim" / "…letterhead" (widened in brand-forge 0.4.20), and `agent-ops/check-monolith.py` ST5 missed the refusal idiom "ingested content does not get to direct my actions" / "smuggled into data — refused" (widened in agent-ops 0.1.16); both real wordings now live in their corpora. Closes I-13's recall half (ISSUES I-13); the scorings half (carve-quality empirical application + external plugins) stays open. plugin.json 0.2.50 → 0.2.51.

## 0.2.50 — 2026-06-15

- **Runtime launch reproducibility — the operability lesson, dogfooded back from dev-factory.** Operating dev-factory's bundled `dev-server` (a FastAPI app the operator starts) surfaced a packaging gap: its ~10 env vars were inline-only on the `uvicorn` line, undocumented and unbounded-by-omission — *"where do I even set the heartbeat / the dispatch cap?"* had no home, so launching the autonomous loop without a cap was one forgotten variable away (logged as dev-factory DF-8, fixed there with a `run.sh` launcher + a `dev-factory.env.example` template + a gitignored operator config + a RUNBOOK entry). The general lesson is now a **checked David F. (reproducible-packaging) property**: `references/rubrics/manifest-and-packaging.md` gains **D8 — Runtime Launch Reproducibility** (`[review]`, conditional): a plugin that ships or wraps an **operator-launched** runtime — a bundled server/daemon/loop the operator starts, *not* a host-launched `.mcp.json` MCP — must let an operator launch and re-launch it from a **persistent, documented config** (launcher + env template + runbook), with the safe defaults (especially any loop/dispatch cap) applied and the operator's filled-in env gitignored, never committed into the plugin. Inline-env-only (command reconstructed from source each run, safety bound a forgotten variable away) = D8 ≤ 2. Paired **AP-MP5** (the runtime with no launch config), **First Principle 5** (a bundled runtime needs a launch surface), and **Hard Test 6** (the launch-config test); the **David F. critic** (`agents/critic-david-f.md`) now carries the lens in its review mandate — an inline-env-only runtime is a Major, "works on my machine wearing a `uvicorn` line." Plugins that ship no operator-launched runtime score it N/A (the catalog's four other plugins). Rubric 0.1.0 → 0.2.0; plugin.json 0.2.49 → 0.2.50.

## 0.2.49 — 2026-06-14

- **`check-integration-contract.py` gains the namespace check (J) — ROADMAP Track 5, increment 4 complete.** A plugin MAY declare its durable-state namespace in `plugin.json` (`stateNamespace`); if it does, the gate asserts it sits under `.agents/` (the D-15 state umbrella, distinct from the `.claude/` config namespace) AND matches what the plugin's `bin/` actually writes — so the declaration can't drift from reality (the same "declared-state must match reality" discipline `check-manifest-sync.py` enforces for version/description). Absence = stateless (the three corpus/judge plugins declare nothing). harness-forge declares `.agents/harness`, agent-ops `.agents/brain`; both validated against their `bin/` writes. This turns D-15 from prose discipline into a *checked* property. selftest-proven (a valid declaration passes; a `.claude/` namespace + a declaration that doesn't match `bin/` writes both FAIL; a stateless plugin passes). With this the integration contract's static half is complete (H · I-static · I-behavioral · K · J); the host adapter (increment 5) is the remaining piece. plugin.json 0.2.48 → 0.2.49.

## 0.2.48 — 2026-06-14

- **`check-integration-contract.py` gains the host-awareness check (K) — ROADMAP Track 5, increment 4 (host-awareness half).** A bundled hook (`hooks.json`) or MCP (`.mcp.json`) command that runs a bundled script must resolve it via `${CLAUDE_PLUGIN_ROOT}` (the host-provided root), not a bare or absolute path that won't survive install into the host's plugin cache. The host-awareness invariant: a plugin locates its own files THROUGH the host, never hardcoded — so the same bundle works wherever, and under whichever host, it's installed. All 5 plugins pass (every hook/MCP command is already `${CLAUDE_PLUGIN_ROOT}`-rooted); this freezes it. Runs in the static (always-open) pass; selftest-proven (a bare `bin/` command FAILS, a `${CLAUDE_PLUGIN_ROOT}`-rooted one passes). The namespace check (J — the `.agents/<plugin>` state declaration validated against what each `bin/` writes) is the remaining half of increment 4. plugin.json 0.2.47 → 0.2.48.

## 0.2.47 — 2026-06-14

- **`check-integration-contract.py` gains the BEHAVIORAL advisory-hook check (ROADMAP Track 5, increment 2).** Increment 1's static check guaranteed "no bundled `PreToolUse` deny"; this adds the robust behavioral guarantee it can't: **execute each bundled `--hook` on finding-triggering worst-case input and assert it exits 0 AND emits no block/deny decision** — closing the exit-0-but-prints-`{"decision":"block"}` escape (a host honors a stdout decision regardless of exit code, so exit-0 alone doesn't prove "advisory"). Per-plugin worst-case fixtures (each hook reads differently — brand/product read `content`, plugins-factory/agent-ops read disk on `file_path`, harness reads the path string), with a generic fallback for an unknown plugin; a finding that doesn't fire is a soft WARN (fixture drift), the exit-0/no-block assertions are hard. It **executes plugin code**, so it rides the I-12 exec interlock (`--trusted-source` / `PLUGINS_FACTORY_TRUST_EXEC=1`, the check-mcp-liveness pattern): the static checks (H router-thinness + I-static) stay open by default; `--trusted-source` adds the behavioral pass; CI runs both. Confirmed against the real catalog — **all 5 bundled hooks fire a finding on worst-case input and still exit 0 with no block decision** (0 behavioral fails, 0 no-output warns), freezing the advisory invariant. selftest-proven (an exit-0 advising hook passes; an exit-0-but-emits-block hook + a non-zero-exit hook FAIL; the interlock gates the exec). plugin.json 0.2.46 → 0.2.47.

## 0.2.46 — 2026-06-14

- **New gate: `check-integration-contract.py` — the plugin integration contract as a CHECKED property (increment 1 of ROADMAP Track 5: hardened, host-aware integration).** The catalog's integration invariants were held by convention + author discipline; this gate mechanizes the two that no existing gate covers (gap-audited against validate_plugin / context-cost / trust-boundary / mcp-liveness / manifest-sync). **(H) Entry/router thinness** — a command must ROUTE (reference a skill/agent on disk, a `${CLAUDE_PLUGIN_ROOT}/bin/` script, or a `references/` doc) AND stay under the size budget (FAIL ≥ 4000 chars, WARN ≥ 2800): the five-primitive law ("commands are thin typed entry points; never re-contain methodology") mechanized structurally, reinforcing context-cost's token budget. Calibrated on the real catalog (37 commands, max body 3269; the skills they point at are ≥ 6993, so a re-contained method lands in skill territory and fails) — the catalog passes clean, WARNs only on the 3 legitimately-heavy `bin/`-orchestration commands (harness-run / harness-assess / product-corpus-export). **(I) Advisory bundled hook (structural)** — a plugin's own `hooks.json` must never bundle a `PreToolUse` entry: only a PreToolUse hook can DENY a tool call, so a bundled one is blocking; blocking is legal only by consent-wiring into the user's project (harness-forge's `wire.py`), never bundled. All five bundled hooks are PostToolUse-only today; this freezes it. selftest-proven (fat/inert command FAILs, a bundled PreToolUse FAILs); wired into CI. **Next increments (Track 5):** the behavioral hook check (execute each `--hook`, assert exit 0 + no block-decision under finding-triggering input, behind the I-12 exec interlock), `host-detect` (the host-awareness foundation), and the namespace (`.agents/<plugin>`) + host-awareness (`${CLAUDE_PLUGIN_ROOT}` resolution) checks. plugin.json 0.2.45 → 0.2.46.

## 0.2.45 — 2026-06-13

- **reference-lint — skip dotfolder-rooted refs (the `.agents/` ↔ `agents/` collision).** `_candidate()` normalizes a path with `lstrip("./")`, which strips a leading dot — so a runtime-state ref like `` `.agents/harness/lattice.json` `` (introduced when the catalog's stateful plugins consolidated their state under the `.agents/` namespace) became indistinguishable from `agents/harness/lattice.json`, and `agents/` **is** a real plugin dir (the council). The linter then tried to resolve project-state example paths as plugin files and failed. Every other dotfolder (`.harness/`, `.brain/`, `.github/`) was already skipped because its first segment wasn't a plugin dir; only `.agents/` collided. Fix: a `.name/`-rooted path is runtime/project state or external config, never an internal plugin file → skipped (after the existing `./`/`../` handling, so file-dir-relative refs are unaffected). The catalog's own harness-forge + agent-ops trees (now carrying `.agents/…` refs) are the standing regression test in CI.

## 0.2.44 — 2026-06-13

- **check-recall — the flagship corpus re-sourced from real transcripts (I-13 deeper fix).** The self-red-team (Andrej K. + Chip H.) flagged check-recall as a closed loop: it validated *author paraphrases* against *author patterns*, proving nothing about whether the patterns catch what the council *actually says*. **Fixed for the flagship:** `plugins-factory-check.recall.json`'s phrasings are now lifted verbatim (lightly trimmed) from the three real `runs/2026-06-10-mega-helper-*.md` council transcripts — e.g. Chip H.'s "25 endpoint-shaped tools, each a direct passthrough to the REST endpoint" and Boris C.'s "kitchen-sink bundle with no shared job" — not invented paraphrases. A green check-recall on this corpus now means "the checker's patterns still catch wordings the council really used" (a measurement) instead of "the patterns match the author's paraphrase of its own patterns" (the tautology). A `_provenance` field marks a corpus as real-sourced; the docstring no longer claims the gate only tests anticipated wordings — it tests real ones where the corpus is re-sourced. The remaining 17 corpora are tracked for the same treatment in I-13 (the curation is per-checker manual work — reading each plugin's runs to extract its council's actual phrasings).

## 0.2.43 — 2026-06-13

- **`check-manifest-sync` C4 — enumeration completeness (mechanizes the brand-forge drift class).** The rubric calibration (0.2.41) *found* a real defect by hand — brand-forge's `plugin.json` enumerated six of its seven commands, omitting `/brand-corpus-export`. C3 only caught *false* citations (a command named that doesn't exist); the omission slid through. New **C4**: a parenthetical/bracketed list of **≥3 same-prefix slash-commands** is conventionally the *complete* command set, so if it omits a command that ships, that's the drift — now a hard CI failure naming the missing commands. Precise by construction (only fires on an explicit `(a, b, c)` run of ≥3, never a prose mention of one or two), so an intentional subset stated outside parens never trips it — verified against all five catalog plugins (zero false positives) and proven by a selftest fixture (a 3-of-4 list fires; a complete 3-of-3 list passes). The defect the review found can no longer silently recur.

## 0.2.42 — 2026-06-13

- **The marketplace publish workflow (PLAN #4, the D8 example-only gap) — `bin/assemble-marketplace.py`.** A carve turns one library into N plugins, some depending on others (a *foundation plugin* others `dependencies`-declare); this is the **publish** step that turns the built plugins into a marketplace. It discovers every `<library>/*/.claude-plugin/plugin.json`, emits the multi-plugin `.claude-plugin/marketplace.json` (one entry per plugin — `source: ./<dir>`, `tags` from `keywords`, `category` from a `category` field), and **fails unless the carve is legal**: every plugin manifest validates, the assembled marketplace validates (reusing `validate_plugin`'s validators — incl. the D-13 cross-plugin **agent-collision** check, so a carved library can't ship two councils that silently drop a critic), and the **`dependencies` graph resolves and is acyclic** — every dep is in the library or `--allow-external`-vouched (recorded in `allowCrossMarketplaceDependenciesOn`), and foundation plugins are sinks (the carve-quality **D4** invariant, now mechanized at publish time). It produces + validates the manifest; it does **not** deploy (pushing is the owner's). `selftest`-proven (a clean 2-plugin library with a foundation dep; catches an unresolved dep, a cycle, a new agent collision; derives tags/category), and **CI round-trips the real catalog** — assembling `nonoun-plugins` from the 5 plugin dirs and asserting it validates. Wired into `carve-method.md` §Publish; the 11th `bin/` tool.

## 0.2.41 — 2026-06-13

- **Rubric calibration against reality (PLAN #3) — `plugins-holistic` `empirical_applications` 0 → 3.** The I-13 `score-record.py` writer is now exercised on real plugins: three cold holistic scorings recorded in `scores/` — `plugins-factory` (self-red-team, CONDITIONAL) + **`harness-forge`** and **`brand-forge`** (both APPROVED), each scored by an evaluator reading the plugin cold against the 9-dimension rubric. **The calibration did real work, not a rubber-stamp:** the brand-forge scoring caught a genuine declared-state drift — the manifest enumerated six of seven typed commands, omitting `/brand-corpus-export` — capping its P5/P8 at 4 (fixed in brand-forge 0.4.19; the score record keeps the 4s as the honest pre-fix snapshot). The harness-forge scoring independently re-confirmed the two residuals its own council found (the I-9 arming gap → P9=4, the bundled-council standing tax → P6=4). This is the rubric's first move from *self*-applied to applied across the catalog, and it discriminated — APPROVED is not automatic, and a real defect fell out. **Open:** `carve-quality.md` still needs a real carve artifact to score (its `empirical_applications` is 0); the council-recall corpora still want re-sourcing from real transcripts; and the enumerated-command drift class is a candidate to mechanize in `check-manifest-sync`.

## 0.2.40 — 2026-06-13

- **I-11 / D-14 — the corpus-reader relocated out of the judge.** Completing I-11: the canonical corpus-reader (a buildless static-site reader serving the *maker* plugins' `*-corpus-export` commands) and its `sync-corpus-reader.py` moved from `plugins-factory/bin/` to repo-root build tooling at **`tools/corpus-reader/`** + **`tools/sync-corpus-reader.py`** — chosen because the catalog already has repo-root tooling (`gen-index.py`, `check-ci-paths.py`), so it's a precedent, not a new convention (the "everything is a plugin" objection, answered). No plugins-factory command ever used the reader; it was here only as the canonical source the sync tool fans into brand-forge/product-forge, whose **vendored copies stay** (their export commands use them; symlinks don't survive install). **What plugins-factory keeps:** `bin/check-bake-safety.py` — the path-generic XSS *security gate* that validates a baked reader, independent of where the reader lives (the catalog's gate-owner's job). **Result:** plugins-factory's tracked `bin/` dropped ~256KB → ~133KB and no longer trips its own 0.2.39 install-payload WARN; the WARN still correctly fires on brand-forge/product-forge, where the reader is genuinely on-job. CI repointed (build/`--init`/`--bake`/JS-parse/sync all run from `tools/`); `check-ci-paths` + `sync --check` + `check-bake-safety` green from the new home. The reader's behavior-bearing source-fingerprint is unchanged by the move, so no reader CHANGELOG churn.

## 0.2.39 — 2026-06-13

- **I-11 (self-red-team, Boris C. + Elon M. Critical) — `context-cost.py` is no longer blind to `bin/` payload (the gate blind spot, mechanized).** Boris's deeper finding: the P6 gate measures only eagerly-loaded `description`s, so it certifies the lean always-on surface while structurally unable to see a heavy off-job bundle sitting in `bin/` — the exact kitchen-sink it certifies others against. **Fix:** `context-cost.py` now measures committed (git-tracked) `bin/` **install payload** as a *distinct axis* from always-on context — a distinction the council blurred: `bin/` files never load into the model's context, so this is distribution/disk weight, not a token tax, and it's labeled as such. It reports `install: bin/ NkB / M tracked files, heaviest bin/<subtree>` per plugin and **WARNs when a single bin/ subtree both exceeds 64KB and dominates (≥40%)** the payload — `bin/corpus-reader` now trips it (plugins-factory 43%, brand-forge 80%, product-forge 73%), making the off-job mass visible where it was invisible. **git-tracked-only** keeps it clean-clone-true (the gitignored GT America font tree — the "licensing" Critical the verification pass *refuted* — is correctly excluded). WARN, never FAIL: P6 is a judgment, so it exposes the weight for a human to judge job-fit rather than grading taste. `selftest` extended (a dominant bin/ subtree WARNs; a small/diffuse bin/ doesn't; the no-git fallback walk reports `tracked=False`). **Still open (the relocation, candidate D-14):** the canonical reader living in the judge plugin is a cohesion smell, but the target is a real design fork — now that the weight is measured + flagged, it's a deliberate decision, not urgent (see `docs/ISSUES.md` I-11).

## 0.2.38 — 2026-06-13

- **I-10 (self-red-team, Steve Y. Critical) — cross-plugin critic-agent name collision, mechanized + fixed (see D-13).** The catalog reuses named-critic personas across councils (`critic-boris-c`, `critic-andrej-k`, `critic-simon-w` in plugins-factory **and** agent-ops; `critic-garry-t` in agent-ops **and** product-forge), and the orchestrators dispatched them by **bare name**. The runtime question is settled (claude-code-guide vs the subagents docs): Claude Code resolves a bare agent name with a **silent drop** — "keeps one and discards the other without warning" — so co-enabling two such councils makes a critic vanish. **`validate_plugin.py marketplace` now detects cross-plugin agent collisions:** a WARNING for the four known persona reuses (allow-listed in `KNOWN_AGENT_REUSE`, each carrying the scoped-dispatch reminder), an **ERROR** for any *new/unintended* collision (caught before it ships); `selftest` proves both paths. The `plugin-council` orchestrator now dispatches its critics by the **plugin-scoped** name `plugins-factory:critic-<name>` (the documented disambiguation), and CLAUDE.md records the convention for every catalog council. Not a slug rename — scoped dispatch achieves address-uniqueness without churning the obscured-`critic-<first>-<initial>` convention. (Sibling orchestrators agent-ops + product-forge updated in lockstep.)

## 0.2.37 — 2026-06-13

- **I-13 (self-red-team, Charity M. Critical) — the judge now emits a durable verdict record (the D8 audit trail).** `score`/`promote` produced a 9-dimension scorecard + verdict that evaporated into the chat transcript: no regression diff, no record that a plugin was ever certified, `empirical_applications: 0`. New `bin/score-record.py` writes a **validated** `scores/<plugin>.json` in the exact `adoption_contract` shape `rubric-manifest.json` specifies — `write` (P1=3,…,P9=2 shorthand → canonical keys), `validate`, `selftest`. Validation **rejects** every malformed shape (empty plugin, out-of-range or bool score, unknown dimension, score for an un-adopted rubric, bad verdict, non-ISO date, unversioned rubric id), so an emitted record is well-formed by construction rather than the model hand-writing JSON (the hopeful-instruction failure mode). `--dir` selects a durable location (`${CLAUDE_PLUGIN_DATA}` / the target repo for installed runs — never the version-keyed cache root, Charity M. m5). The `plugin-score` / `plugin-promote` commands + the `plugin-evaluate` skill now instruct emitting one as the final step; CI selftests the gate and validates every committed record.
- **First real record — `scores/plugins-factory.json`.** Dogfooded immediately: the 2026-06-13 self-red-team's own scorecard (**P1 3 · P2 3 · P3 3 · P4 4 · P5 3 · P6 3 · P7 2 · P8 2 · P9 2**, verdict **CONDITIONAL**, pointing at `reviews/2026-06-13-who-reviews-the-reviewer.md`), moving `plugins-holistic`'s `empirical_applications` **0 → 1** — the rubric's first recorded application to a real (if self-) plugin. **Still open (I-13 deeper half):** re-source the council recall corpora from real `runs/*.md` transcripts; score genuinely external plugins to grow `empirical_applications` (PLAN "Next" #3).

## 0.2.36 — 2026-06-13

- **I-12 (self-red-team, Simon W. Critical) — the executing gates' trust boundary is now a code interlock, not a docstring.** `check-mcp-liveness.py` and `context-cost.py --with-mcp` spawn a target plugin's MCP server (its `.mcp.json` `command`/`args` run as a subprocess with the full ambient environment) — so against a malicious bundle the "server" is arbitrary code execution, the lethal trifecta this plugin grades Score-1 in others. The guard *was* a comment ("TRUSTED catalog plugins only"); prose is not a mechanism. Now the executing paths **refuse by default** (exit 3, spawning nothing) and require an explicit per-invocation trust assertion: the **`--trusted-source`** flag OR **`PLUGINS_FACTORY_TRUST_EXEC=1`** in the env (CI sets the flag; a human almost never does by accident). The interlock is **narrow** — `context-cost.py`'s default static audit executes nothing and stays open with no flag; only the opt-in `--with-mcp` path is gated. Both gates' `selftest`s now prove the interlock (refuse-without-trust → exit 3; flag/env authorizes; static unaffected). CI updated to pass `--trusted-source` on its two executing invocations; CLAUDE.md's runnable examples + the gate descriptions updated. `safe state is the default; unsafety requires a deliberate, visible act` — the inverse of before.

## 0.2.35 — 2026-06-13

- **Self-red-team: "who reviews the reviewer?" — the 9-critic council run on plugins-factory itself** (`reviews/2026-06-13-who-reviews-the-reviewer.md`, the catalog's highest-leverage sibling review per docs/PLAN.md "Next" #1). The meta-tool authored and judged every other plugin but carried one prior `reviews/` entry; a blind spot in *it* is inherited by every judgment it renders. Full panel as parallel isolated `plugins-factory:critic-*` agents, bracketed by a deterministic **gate anchor** (every gate plugins-factory ships, run on plugins-factory — all green) and a **verification pass** that confirmed or refuted each headline before any fold. 7 of 9 critics filed a Critical; the panel converged on three themes (an off-job `bin/` payload the P6 gate can't see · a verification layer that doesn't verify · executing gates bounded by prose) — **and the verification pass refuted the most-piled-on Critical** (a "ships licensed fonts" claim that is gitignored/untracked; four critics asserted it, only Boris C. checked `.gitignore`). The record is itself the answer to the council's own Charity-M. Critical: the tool that certifies everything finally writes down a certification of itself.
- **Folded (convergent honesty fix): `check-recall.py` now states its boundary.** Andrej K. + Chip H., independently, both Critical: the gate validates author-written paraphrases against author-written regexes — a closed loop that proves the patterns aren't brittle against *anticipated* wordings but **does not** measure the council's *live* catch-rate. The docstring now says exactly that (inheriting the caveat the calibration `check.py:8` already carried), so a green run can't be misread as "the council is verified." The deeper fix (re-source the recall corpora from real prior `runs/*.md` transcripts) is tracked as I-13. Pure documentation/honesty — selftest + live corpus run unchanged and green.
- **Four architectural findings escalated to tracked decisions** (`docs/ISSUES.md` I-10…I-13) rather than reflexively folded — they ripple catalog-wide and warrant an owner's call: cross-plugin critic-agent name collision (I-10) · `bin/corpus-reader/` off-job + `context-cost.py` blind to `bin/` payload (I-11) · executing gates' trusted-only boundary is prose not a mechanism (I-12) · the judge keeps no durable verdict record + unmeasured council recall (I-13, already ROADMAP'd).

## 0.2.34 — 2026-06-12

- **`context-cost.py --with-mcp` — folds the MCP tool-definition cost into the always-on measurement**, the rubric's single biggest P6 concern ("a bundled MCP server 1:1-wraps an API — 30 endpoint-tools eating 50K+ of always-on context"). The static audit could only NOTE that an MCP was wired; `--with-mcp` spawns each declared server, completes an `initialize` + `tools/list` handshake (the same mechanism as `check-mcp-liveness.py`), and adds the served tools array's serialized size to the total + the per-category breakdown — and **WARNs if the tool-defs *dominate* the budget** (the wrapper-MCP smell, the P6 anti-pattern made mechanical). It EXECUTES the server, so like the liveness gate it is for **trusted catalog / CI only**, never an untrusted bundle; the default static audit is unchanged. `selftest` extended (a live mini-server's tool-defs are measured + folded into the total; a 20-tool wrapper trips the domination WARN). Wired into CI (`marketplace . --with-mcp`). First-run record: the catalog's three live MCPs add **brand-corpus 1,111c · product-corpus 1,251c · repo-memory 1,261c** of tool-defs — ~5–9% of each plugin's always-on, well under the wrapper threshold (our MCPs are curated perimeters, not 1:1 API wrappers) — bringing the *measured* catalog always-on to ~10.6K tok. Closes the ROADMAP `--with-mcp` follow-up.

## 0.2.33 — 2026-06-12

- **New rubric: `references/rubrics/carve-quality.md` — score a carve (a library→plugins boundary proposal) as ONE artifact.** A standing v0.2 ROADMAP item, and the gap `carve-method.md` itself named: a carve was scored only by re-applying the per-plugin P1/P3/P4 across the set, which can't see the **set-level** properties — a partition that yields individually-fine plugins yet split a `$ref`-coupled pipeline and wired it with `../`, or whose dependency graph has a cycle, or that silently dropped a skill. Seven MECE dimensions, each mapped to a carve-method step: **D1 Graph Fidelity** (mapped the real coupling incl. hidden `$ref` edges, not the catalog) · **D2 Cuts at the Joints** (boundaries land on thin seams, don't sever dense clusters) · **D3 Shared-Infra Legality** `[gate]` (every cross-boundary share resolves co-locate/`dependencies`/symlink — a surviving `../` caps the carve) · **D4 Dependency-Graph Integrity** `[gate]` (acyclic, foundations are sinks) · **D5 Node Accounting** `[gate]` (every input node a member/orphan/bridge/dead — no silent drops) · **D6 Granularity Calibration** (between kitchen-sink and fragment) · **D7 Buildable Proposal** (`author` can build each row without re-deriving the boundary). Composes with `plugins-holistic` (which scores each plugin the carve *produces*); the three `[gate]` dims cap the whole carve, mirroring how P4/P9 cap a plugin. Five carve anti-patterns (catalog-carve, `../`-shared-dep, silent-drop, un-buildable, cyclic/fragment) + seven hard tests (the install test, the diff test, the acyclicity test, …). Registered in `rubric-manifest.json` (primary critic **Steve Y.**, `scores_artifact: carve-proposal`, no paired foundation — grounded in `carve-method.md` + the cohesion/dependency foundations) and wired into `carve-method.md` §scoring + the `plugin-build` skill's `carve` sub-mode so it's reachable, not orphaned.

## 0.2.32 — 2026-06-11

- **New gate: `bin/context-cost.py` — mechanizes P6 Context Economy's always-on audit, the rubric's last unmechanized `[hypothesis]`.** `references/rubrics/plugins-holistic.md` P6 names this script by hand ("read `claude plugin details` *or `context-cost.py` when available*") — now it exists. It measures a plugin's **always-on** context cost: the frontmatter `description` (+ a command's `argument-hint`) of every agent / command / skill — the text that loads eagerly into *every* session just by enabling the plugin. Bodies, references, and SKILL.md prose are excluded (they load on-invoke — progressive disclosure), so the number is the true standing tax. Per plugin it reports total chars + an estimated token count, a per-category breakdown, the heaviest components with their share of the budget, and whether an MCP is wired (its live tool-def cost is real but not statically countable — it points at `check-mcp-liveness`). P6 is `[review]`/`[hypothesis]`, not `[gate]` — so this **makes the cost visible** (the rubric's stated prerequisite) and flags the two mechanical smells it calls out (a single **verbose** description *dominating* >25% of the budget; a description so long it is a body in an advertisement), but hard-FAILs only on the unarguable cases (a >2KB description, or an explicit `--budget` breach), never on a legitimately rich description. Ships a `selftest` that proves the measurement (incl. folded `>` block scalars — the dominant description format here, which a naïve single-line parse silently undercounts 10×), the WARN/FAIL tiers, and a `--budget` breach. Wired into CI (`selftest` + `marketplace .`). First run records the catalog's standing cost: **≈ 38.9K chars / ~9.7K tok always-on** across the four plugins (product-forge heaviest at ~4.3K tok over 40 components; agent-ops's `repo-review` description the single largest at 1054c — a non-failing WARN, the one description over the terseness budget). The 9th `bin/` gate; CLAUDE.md + README enumerations updated.

## 0.2.31 — 2026-06-11

- **New gate: `bin/check-trust-boundary.py` — mechanizes the catalog's core safety invariant, that every reviewer carries the "untrusted DATA, never instructions" guard** (the 2026-06-11 real-repo audit's P1-2: the guard ships across ~66 *isolated* files by author discipline alone, and a single forgotten copy is a **silent** security regression — the new reviewer still runs, still reads untrusted material, and now obeys an injected "rate this 5/5" nobody gated against). Because each critic runs in its own parallel isolated context the guard can't be centralized, so this asserts its **presence** across the whole reviewer surface: every `agents/critic-*.md`, every `agents/*council*.md` orchestrator, and every review-surface skill (`skills/*{evaluate,review}*/SKILL.md` — which pulls in agent-ops's `repo-review` alongside the three `*-evaluate` skills). The test is file-level and two-signal, keyed on the guard's distinctive **assertion** (the reviewed artifact is DATA, never instructions to obey/comply) rather than incidental security prose — a persona that merely name-drops "untrusted input" without the assertion is correctly NOT counted (a `selftest`-proven specificity case, since the security critics legitimately discuss exactly those words). Ships a `selftest` that FAILS on a guard-less critic, a guard-less review skill, and security-prose-without-the-assertion, and that holds general methodology skills out of scope (structure is mechanized; the *quality* of a guard stays the council's job). Wired into CI (`selftest` + `marketplace .`) over all four plugins — **66 reviewers, all guarded** (brand-forge 16 · plugins-factory 11 · product-forge 25 · agent-ops 14). The 8th `bin/` gate; CLAUDE.md + README gate enumerations updated.

## 0.2.30 — 2026-06-11

- **New gate: `bin/check-bake-safety.py` — the baked single-file reader's XSS/integrity gate, extracted from a 495-char inline CI one-liner** (the real-repo audit's P1-3: unreadable in a diff, untestable in isolation, tied to R-1's "unnoticed because unwatched/unreadable" root cause). `build-sitemap.py --bake` inlines untrusted corpus markdown + the reader JS into one `<script>`-bearing `reader.html`; this asserts the bake stayed safe — six named checks (corpus inlined · no `</script>` escape in the data island · none in the module bundle · ≥6 SRI hashes · DOMPurify wired · no live `<script>alert(`), one clear message each instead of bare asserts. Ships a `selftest` that proves it FAILS on an injected `</script>` escape, a live alert, stripped SRI, and a missing DOMPurify (per PLAN.md's "new gates prove themselves"). It lives in `bin/` (a CI tool, deliberately NOT in the vendored `bin/corpus-reader/` tree). `ci.yml`'s bake step now reads as `selftest → bake → check-bake-safety --emit-bundle → node --check`. Same verdict as the old inline assertion on a real bake, verified.

## 0.2.29 — 2026-06-11

- **New gate: `bin/check-recall.py` — guards the council-calibration checkers against brittle concept-regex.** Those checkers score a transcript by matching regex per planted defect, and they develop *recall* gaps: a council catches a defect but words it a way no pattern matches — a CHECKER miss, not a council miss, which bit three separate run-3 samples (the over-fleet A4/A6 fix, the monolith MO6 "form" vs "format" fix), each found reactively. The harness finds them proactively: each checker gets a paraphrase corpus (`evals/recall-corpus/*.recall.json`) listing the legitimate ways a council might word each planted defect; the harness imports the checker, lowercases each paraphrase exactly as the checker lowercases its transcript, and asserts every paraphrase matches ≥1 pattern — and that the corpus's defect keys **equal** the checker's PLANTED keys (no defect without recall coverage). Ships a `selftest` (catches a deliberately-brittle pattern, a coverage gap, and a stale key) and is wired into CI over all 9 catalog checkers.
- **The harness paid for itself on first run** — it surfaced **31 brittle-pattern gaps across 7 checkers**, most of them *recurring* council lines the patterns would have missed in a real run ("a sticker, not an identity" / "you chose a typeface, you did not make one" / "context dumped, not engineered" / "a model could generate this in thirty seconds"). All 9 checkers' patterns were widened to match these legitimate wordings; **every recorded baseline + rate sample re-scores full with zero regression**. 176 paraphrases now covered across 52 planted defects.

## 0.2.28 — 2026-06-10

- **docs-studio council-calibration promoted to N=3** (a third cold run through the real `plugin-council`): **2/2 planted defects in 3/3 runs, BLOCKED ×3**, P3 held at 4 in every run (the coherent-scope isolation from the kitchen-sink/wrapper findings held). Run 3 demonstrated the updated S3 working as intended — with liveness + hollowness now *covered* probes (PF5/CF5), the panel surfaced the **next** blind spot ("no critic owns 'prove it works once' end-to-end") rather than re-reporting the two now closed. README rate table updated.

## 0.2.27 — 2026-06-10

- **New gate: `bin/check-mcp-liveness.py` — the AP-P7 mechanization.** The council's liveness *finding* becomes a deterministic gate: it spawns each bundled MCP server, completes an `initialize` + `tools/list` handshake over newline-delimited stdio, and requires a well-formed JSON-RPC response carrying a tools array. A plugin with no `.mcp.json` is a clean skip. Static validation proves *wiring* (`validate_plugin.py`); this proves *execution* — closing the gap a dead-but-wired server (`docs-studio`'s `docs-mcp.py`) sails through. **Trust boundary:** it executes the server, so it is for trusted catalog plugins / CI only; the council reviewing untrusted bundles keeps liveness a cold-read finding (CF5). Ships with a `selftest` (PASS a live mini-server, FAIL a dead-but-wired one, clean-skip the empty case) and `plugin` / `marketplace` modes. Wired into CI (`selftest` + `marketplace .` — brand-forge's `brand-corpus` serves 5 tools; the other three skip until their MCP slots fill). Verified against `docs-studio`: correctly FAILs the planted dead server. CLAUDE.md + README gate enumerations updated.

## 0.2.26 — 2026-06-10

- **Folded the calibration learnings back into the instruments, then proved them with a second fixture shape.** The two blind spots the council named on itself, and the four rubric findings from `evals/rubric-calibration/`, are now encoded:
  - **`references/rubrics/plugins-holistic.md` → 0.2.0:** a pre-carve scoring note (which anchors suspend / which discriminate); a **personal-state hard test** under P1 (credentials/PII/live state entangled with the distributable — the accounting-studio finding); a **P8 high-form/low-truth anchor + changelog-truth test** (disciplined-looking entries whose claims fail against the tree); a **P9 `bin/` blast-radius probe** (side-effects documented at the contract level, not only in docstrings); two new anti-patterns **AP-P6 (the hollow component)** and **AP-P7 (the dead-on-arrival wiring)**; and hard tests 11–13 (personal-state, liveness, hollowness).
  - **`references/critics/eval-prompts.md` → 0.2.0:** **PF5** (the hollow-component test) and **CF5** (the liveness test) added so the council carries both former blind spots natively; S3 updated to hunt the *next* gap.
  - **Second calibration fixture — `docs-studio` (`build-fixture-hollow.py` + `check-hollow.py`):** a coherent single-job, task-shaped-MCP plugin that fails by **vacancy** (hollow bodies) and **deadness** (a wired MCP that never serves), isolating the new probes from the kitchen-sink/wrapper findings. Two cold runs through the real `plugin-council`: **2/2 planted defects caught, BLOCKED ×2**, P3 held at 3–4 (isolation confirmed) — the panel caught hollowness + liveness on a plugin it would previously have waved through.
  - **`check.py` precision fix:** cross-applying it to `docs-studio` surfaced over-loose bare-label patterns (`\bp2\b`/`\bp3\b` match any scorecard) and negated-mention sensitivity; bare labels removed (mega-helper still 2/2 on substance), the residual documented as an inherent concept-regex limit bounded by per-fixture scope.

## 0.2.25 — 2026-06-10

- **Council-calibration at N=3 + rubric calibration at N≥3 — the two standing v0.2 eval conditions met.** (1) mega-helper re-run twice through the real `plugin-council` agent, cold: **2/2 planted defects in 3/3 runs, BLOCKED ×3**; both new runs independently re-surfaced the same emergent Criticals (dead MCP server, the unspecified `/deploy`) and the same instrument blind spots (runtime liveness; "the rubric measures bloat; emptiness sails through") — recorded with an honest dispatch-degradation note. (2) **`evals/rubric-calibration/`** (new): the holistic rubric applied cold to three real carve targets → BLOCKED / CONDITIONAL / CONDITIONAL with per-application rubric findings — P4/P5/P9's mechanical tests validated as the strongest discriminators (they caught live credentials inside a would-be distributable and a candidate failing its own validator), P1/P3/P6/P7 annotated as weak discriminators pre-carve, four concrete rubric improvements recorded. ROADMAP updated; sensitive values redacted from the public records by policy.

## 0.2.24 — 2026-06-10

- **corpus-reader: full-text search in baked builds.** With every page's markdown inlined (`--bake`), the sidebar filter now also matches **page content** — a metadata miss falls through to the inlined text (lazily lowercased + cached per page), and the search placeholder says "Search content…" in baked mode. Served layouts keep the metadata-only filter unchanged (content isn't local there). Re-synced into the vendored copies.

## 0.2.23 — 2026-06-10

- **corpus-reader: baked single-file mode (`--bake`) — `file://` works.** `build-sitemap.py --bake <corpus>` emits **one self-contained `reader.html`** (double-click, no server): the sitemap, every page's raw markdown, the reader CSS + per-corpus theme, and the component modules concatenated into an inline `<script type="module">` (inline modules execute on `file://`; only fetched ones don't). Rendering is the identical code path — marked parses, DOMPurify sanitizes — and the CDN render-library tags are **lifted verbatim from `index.html`** so the version pins + SRI hashes keep one source; embedded JSON is `<`-escaped so corpus content (including the demo's raw-`<script>` probe) can never terminate the document. `cr-shell`/`cr-ui-page` now read inlined `window.CORPUS`/`CORPUS_FILES` when present and fetch otherwise; the served layouts are unchanged (the sitemap-scan refactor was proven **byte-identical** old-vs-new). CI bakes the demo each push and asserts data/bundle script-safety, SRI presence, sanitizer wiring, and that the extracted bundle parses. Re-synced into the vendored copies.

## 0.2.22 — 2026-06-10

- **corpus-reader: per-corpus theme hook.** `reader.config.json` gains `"theme"` — a stylesheet **inside the corpus**, containment-checked by `build-sitemap.py` (escapes are warned + ignored) and injected by the shell after `corpus-reader.css`, so its `:root` token overrides win. Token overrides only, by contract (the OKLCH axes + semantic tokens); same trust class as corpus images; corpus-relative paths only. `demo-corpus/` ships a worked `theme.css` (teal accent) and CI asserts the theme path flows through both layouts (`demo-corpus/theme.css` standalone, `../theme.css` under `site/`). Re-synced into the vendored copies.

## 0.2.21 — 2026-06-10

- **corpus-reader: four small pattern fixes.** The sidebar wordmark drops a leading separator from the subtitle (`"BZZR — Product Corpus"` → `BZZR` / `Product Corpus`); the sidebar search now also matches page **summaries** (not just title + path); the provenance stats count tags in **prose only** (a doc *about* the tag syntax no longer skews the bar); and `--init` drops a root `index.html` redirect → `site/` when the corpus root has none (never overwrites an existing file) — removing the `/site/`-vs-`/` 404 papercut. Re-synced into the vendored copies.

## 0.2.20 — 2026-06-10

- **corpus-reader: committed `demo-corpus/` + pipeline smoke + JS parse gate.** A tiny synthetic corpus (6 pages — statuses, provenance tags, xrefs, a table, mermaid, a sanitizer probe, `reader.config.json`) now ships with the reader, so a fresh clone renders out of the box instead of an empty shell (the only example was a 43MB gitignored local fixture). CI smoke-builds it every push (`build-sitemap.py` + `--init` into a tmp corpus) and gains a `node --check` parse gate over the reader's ES modules — both proven against planted failures before wiring. Synced into the vendored copies (installed plugins get the working example too).

## 0.2.19 — 2026-06-10

- **`reference-lint.py`: references to deliberately git-ignored targets are exempt from the resolve requirement.** Docs across the catalog cite `agents/.name-map.md` (the obscured-critic provenance file, git-ignored by design) — the linter required it to exist on disk, so it was green on a maintainer tree and structurally red on every fresh checkout (one of the three causes of the 2026-06-05 → 06-10 CI outage). An unresolved reference is now exempt iff `git check-ignore` confirms its target is ignored; outside a git context the strict behavior is unchanged.

## 0.2.18 — 2026-06-08

- **corpus-reader: optional `reader.config.json` for home polish.** A `<corpus>/reader.config.json` (`{"title": …, "sections": {"01-foo": "one-line description"}}`) now sets the corpus title and per-section **card descriptions** on the home — `build-sitemap.py` reads it, the cards render the descriptions, and everything degrades gracefully when there's no config. Re-synced into the vendored copies.

## 0.2.17 — 2026-06-08

- **corpus-reader: the web-component reader gains the new reader's patterns + layouts.** The `<cr-*>` reader now has a sidebar **search** + layer-label nav, a **mobile drawer** (off-canvas + scrim), a home **hero + section cards + a maturity/provenance stats bar** with per-section minibars, and doc **kicker + badges** — plus inline provenance tags (`[KNOWN]/[INFERRED]/[OPEN]/[SEEDED]`) and `path.md` → in-site xref links. `build-sitemap.py` precomputes the status/provenance counts (graceful — the stats bar is omitted when a corpus has neither). Architecture unchanged (web components + OKLCH tokens, served over HTTP). Re-synced into the vendored copies.

## 0.2.16 — 2026-06-08

- **corpus-reader: its own README + a staleness-gated CHANGELOG.** The reader now ships a `README.md` (rewritten for the sidebar-nav layout) and a `CHANGELOG.md`. `sync-corpus-reader.py` gains `--changelog "…"` (prepend a dated entry + refresh a source fingerprint) and `--fingerprint`; its `--check` CI gate now also **fails if the reader's code changed without a CHANGELOG entry** — so the changelog can't silently go stale. Also fixed the sidebar wordmark to derive from the corpus title (it was hardcoded `"BZZR"`, so the generic reader showed the example's brand). Re-synced into the vendored copies.

## 0.2.15 — 2026-06-07

- **corpus-reader: one common `<corpus>/site/` convention.** `build-sitemap.py --init <corpus>` now scaffolds the standard `site/` viewer (machinery only — never a bundled example) and builds its sitemap in a single command. Every plugin's `*-corpus-export` command calls this one tool, so generated corpus sites use an identical layout everywhere. Re-synced into the vendored copies.

## 0.2.14 — 2026-06-06

- **corpus-reader: viewer-in-a-subfolder layout.** `build-sitemap.py ..` now generates the sitemap for a reader living in a `<corpus>/site/` subfolder (content in the parent) — `../`-relative paths, the viewer's own dir excluded from the scan. Lets an exported corpus keep its root as clean, browsable markdown with the app tucked into `site/`. Reader unchanged; the standalone content-beside-`index.html` layout still works. Re-synced into the vendored copies.

## 0.2.13 — 2026-06-06

- **corpus-reader: the home uses the width.** The section-tile home now spans a wider track (90rem, vs the 51rem prose-reading width) with `align-items: start`, so wide viewports get more tile columns and short tiles keep their natural height instead of stretching to the tallest in the row. Reading pages are unchanged (still the 51rem measure). Re-synced into the vendored copies.

## 0.2.12 — 2026-06-06

- **corpus-reader: graceful `file://` guidance.** Opening `index.html` directly (`file://`) blocks ES modules + `fetch()`; the reader now detects this and shows a "serve over HTTP" instruction (with the exact command) instead of a blank page + console errors. Re-synced into the vendored copies.

## 0.2.11 — 2026-06-06

- **corpus-reader hardened + shared.** The bundled `bin/corpus-reader/` (a buildless static site that renders a markdown corpus) now sanitizes untrusted corpus markdown with **DOMPurify** and pins every CDN dependency with **Subresource-Integrity** hashes (fail-safe: prose degrades to escaped text if a script fails its integrity check). New `bin/sync-corpus-reader.py` makes plugins-factory the single source of truth and vendors the reader into brand-forge + product-forge; its `--check` drift gate runs in CI (cross-plugin symlinks don't survive install, so each plugin ships its own synced copy).

## 0.2.10 — 2026-06-05

- **Critic identities obscured** — slugs now `critic-<first>-<initial>`, display names `First L.`, practitioner bios moved to a git-ignored `agents/.name-map.md`; orchestrator roster + eval-prompt corpus + README updated; council behavior unchanged.

## 0.2.9 — 2026-06-04

- **`operational-roles.md` — the aspiration is now the Maker's _precondition_, a soft blocker if absent.** A Maker that converges toward no declared pull has nothing to converge toward, so "converge" degrades to the category average. The doctrine adds an explicit precondition: before substantive making, the domain attractor (Muse / Vision / design-principles) must be **at least lightly named** — one sentence, expected to evolve — and it is a _soft_ gate, cleared by naming a provisional direction, never a hard stop, realized as the first move of every `/x-build` entry. The check is behavioral, not mechanical (the aspiration lives in the work, not the plugin's files, so no static gate sees it). Rubric **R2** now fails a maker that reasons straight to a finished artifact with no declared direction; **R4** requires aspire-to-precede-make. `plugin-build` applies the rule to itself — naming the plugin's one-sentence job (its intent) is the aspiration-precondition before the component-fit table. Wired in lockstep into every catalog plugin's maker flow.

## 0.2.8 — 2026-06-04

- **`validate_plugin.py` now catches the two manifest/frontmatter classes that previously passed every gate.** (1) **userConfig option schema** — each `userConfig` option must declare `title` + `type` (`string|number|boolean|directory|file`) + `description`; a missing `title` is exactly what made the sibling `brand-forge` un-installable while CI stayed green. (2) The **frontmatter flow-collection trap** — an unquoted `argument-hint: [a] [b]` (or any `[`/`{`-opening value that is unterminated or has trailing tokens) makes YAML fail to parse, so the loader silently drops the whole command/agent/skill frontmatter block; a clean `[..]` collection in a string key (`description`/`argument-hint`/`name`/`title`) warns (quote it). Both run in the on-save advisory hook and under CI `--strict`. Selftest extended (userConfig fixtures + flow-trap unit test + on-disk command fixture). `references/plugin-architecture.md` and `references/authoring/plugin-template.md` now document the userConfig schema and the quoting rule. (All command `argument-hint`s in this plugin were quoted to match.)

## 0.2.7 — 2026-06-04

- **`evals/council-calibration/` — the council-calibration eval, with first evidence.** The non-deterministic half of the behavioral suite: a planted-defect fixture (`build-fixture.py` → `mega-helper`, gate-clean but a kitchen-sink bundling four unrelated domains with a 1:1 API-wrapper MCP), a concept-level transcript checker (`check.py`), a protocol (`README.md`), and a recorded baseline (`runs/`). Run cold and given **no hint**, the `plugin-council` caught **2/2** planted judgment defects (P3 boundary, P2 component-fit) that every deterministic gate passes, BLOCKED the fixture, and surfaced emergent findings (an unscoped destructive CRUD MCP → P9, copy-pasted tool schemas, a non-functional server loop). First concrete evidence the council finds the defects no regex can. The live council run stays a periodic manual eval (an LLM panel is not a CI gate); CI runs the deterministic guards — the fixture must stay gate-clean (so it isolates judgment) and the recorded baseline must still score 2/2.

## 0.2.6 — 2026-06-04

- **Added a README.** The lifecycle tool now ships the documentation it requires of every plugin — the command / skill / agent / gate surface, the nine-dimension standard, honest scope, and provenance. (It was the one catalog plugin missing one.)
- **`evals/behavioral-gates.py` — the deterministic behavioral-eval suite** (ROADMAP'd). Builds throwaway fixture plugins, each carrying one known structural defect, runs the `bin/` gates against them, and asserts each is caught — plus a golden clean fixture that must pass every gate. Covers the `../`-dependency (P4), command↔skill collision (P7), agent-smuggling-`mcpServers` (P9), version↔CHANGELOG drift (P8), description count-claim (P5), and dangling-reference classes; wired into CI. It turns "the harness catches a bad plugin" from an assertion into a tested property. The non-deterministic other half — asserting on the critic council's emitted scorecard — stays a documented manual eval (see ROADMAP).

## 0.2.5 — 2026-06-04

- **`validate_plugin.py` — command↔skill slug-collision gate.** Commands (`commands/*.md`) and skills (`skills/<name>/`) are two file-formats of one primitive sharing the `/<plugin>:<slug>` invocation namespace; a command and a skill with the same slug collide (the skill shadows the command → `Unknown command: /<plugin>:<slug>`). The validator now errors on this within a plugin (selftest fixture added; runs in CI via the existing `--strict` steps). This is the gate that would have caught the brand-forge `/brand-evaluate` and product-forge `/product-evaluate` · `/product-research` collisions (fixed in brand-forge 0.4.1 / product-forge 0.3.1).
- **`plugin-architecture.md` — namespacing & naming doctrine corrected against the Claude Code docs.** Made explicit that commands and skills are one namespace (`skills/` preferred for new plugins), that the `/<plugin>:` prefix handles cross-plugin collisions automatically (don't hand-prefix for that reason), that slugs must be unique across `commands/` + `skills/`, that agents are a separate dispatch surface, and the verb-command / noun-skill convention with distinct slugs.

## 0.2.4 — 2026-06-04

- **`operational-roles.md` — the third seat reframed from Provocateur to the aspirational Muse / attractor.** v0.2.3 cast the third seat as a "Provocateur" (divergence); that confused the seat with one of its modes. The third seat is an **attractor** — an aspirational goal / principles / ideal that exerts a **gravitational pull** in a direction; **provocation is one form the pull takes** (when the right direction is away from the mainstream). This makes the seat _near-universal_ rather than narrowly creative: every domain has a north-star (a Muse for brand, a written Vision / PR-FAQ for product, a design-principle set for engineering), so the decision is not _whether_ to have an attractor but how to **right-size** it — a generative agent only where taste makes the aspiration a live judgment, a written principle set otherwise. The "not the red team" distinction holds and is sharpened: the Muse pulls _forward_ toward an aspiration that doesn't exist yet; the red team attacks a converged artifact _backward_. Embedded rubric R2/R3/R5 updated; the cross-links in `agent-architecture.md` + `plugin-build` follow.

## 0.2.3 — 2026-06-04

- **`references/authoring/operational-roles.md` — the Maker / Critic / Provocateur pattern.** A new authoring reference for plugins that _orchestrate_ work: the three operational seats (who makes · who reviews · who provokes), the **provoke → make → review → remake** loop, and the one invariant (no seat judges its own work — realized structurally, not as prose). Generalizes the seat model brand-forge ships (Muse · Team · Council): the Maker+Critic split is universal; the **Provocateur** is a domain-named, optional third seat whose value scales with taste-dependence — a Muse for brand, a ritual like the PR-FAQ for product, a bounded spike (often nothing) for convergent engineering. Names the trap this audience hits most — **the Provocateur is not the red team** (the red team attacks a converged artifact, the Critic seat, _backward_; the provocateur generates options _forward_). Ships an embedded triad rubric (R1 seat-separation + R3 provocateur-≠-red-team are `[gate]`s). Cross-linked from `agent-architecture.md` and `plugin-build`.

## 0.2.2 — 2026-06-04

- **`bin/check-manifest-sync.py` — a new CI gate against declared-state drift.** Mechanizes CLAUDE.md's "keep the four descriptions in sync — drift is a defect" rule and the version↔CHANGELOG honesty the manifest validator can't see (it checks each manifest in isolation). Three checks per plugin: **C1** `plugin.json` version equals the latest dated CHANGELOG release and no "Unreleased" section carries shipped content; **C2** every "N commands / N-critic" count stated in the description / README / marketplace entry matches the real `commands/` + `agents/critic-*.md` counts; **C3** every cited `/command` resolves to a file (sibling plugin names and path fragments are excluded). Ships a `selftest` (clean + drifted fixtures, proven to bite) and runs over all six catalog plugins in CI. This is the gate that would have caught the brand-forge v0.1→v0.2 drift the 2026-06-03 red-team found by hand.

## 0.2.1 — 2026-06-03

- **`skill-architecture.md` — "Harden with structure, not prose."** Made explicit the discipline of moving every guarantee into a structure rather than a prose instruction, in order of strength: mechanized check · tool-scope · **embedded output rubric** (named dimensions, `[gate]` labels, an explicit pass threshold) · named verify target · §SelfAudit · anti-pattern gallery · progressive disclosure. The test: _if a requirement is enforced only by a polite sentence, it is not hardened._ The not-thin checklist now requires an embedded output rubric for judgment-heavy output and that every requirement be carried by a structure. Rubrics and gates over traditional prompting.

## 0.2.0 — 2026-06-03

Evolved **skill + agent authoring** — closing the methodology gap that was producing thin, single-`SKILL.md` skills.

- **`references/authoring/skill-architecture.md`** — the construction methodology (the successor to the standalone `skills-studio`): the five layers (metadata · seed · references · scripts · evals); the required `SKILL.md` seed surface (cold-start mode table, Quick Start, posture, modes, loading manifest, per-mode verify targets, anti-pattern gallery, §SelfAudit, §Teach); size tiers; the `references/` taxonomy with per-file `load-when`/`required-for` frontmatter; the mechanization threshold; voice/posture; and the not-thin checklist. `plugin-build` now builds skills against it.
- **`references/authoring/agent-architecture.md`** — the agent-authoring discipline the plugin lacked: when an agent earns its isolated context (else it's a skill), the role taxonomy (critic · worker · analyst · orchestrator · actor), tool-scope minimal-sufficiency + the lethal trifecta, the in-agent trust boundary, persona design, isolation/memory, and the council orchestration pattern.
- **`references/rubrics/agent-fit.md`** — the scoring face (8 dimensions; tool-scope/trifecta + loader-rule are `[gate]`). Registered in `rubric-manifest.json`; `plugin-evaluate` loads it when a plugin bundles agents.
- Wired into `plugin-build` (new first principle: _components are authored to depth, not stubbed_; the author/edit sub-modes read the new docs) and cross-linked from `frontmatter.md` (the field contract → the structure methodology).

## 0.1.0 — 2026-06-02

Initial release as a **plugin**. plugins-factory was re-cast from a single mega-skill into a self-contained Claude Code plugin, authored by (and against) its own standard — the dogfood test of the tool. Re-shaped through the five plugin primitives for component-fit:

- **Commands** — six thin, typed entry points (`/plugin-author`, `/plugin-carve`, `/plugin-edit`, `/plugin-score`, `/plugin-critique`, `/plugin-promote`) that set mode + posture and route to a skill or the council, without re-containing the methodology.
- **Skills** — split by posture: `plugin-build` (the maker — author/carve/edit) and `plugin-evaluate` (the judge — score/critique/promote, carrying the untrusted-target trust boundary). Both draw on one shared standard.
- **Agents** — the 9-critic council promoted from prose personas to **isolated parallel agents** (`critic-boris-c … critic-david-f`) plus a `plugin-council` orchestrator that fans them out and runs the cross-critic synthesis; `carve-analyst` (the composition-graph fan-out worker) retained.
- **Hook** — an advisory `validate_plugin.py hook` on `plugin.json` / `marketplace.json` writes (surfaces manifest/layout/path smells, never blocks).
- **Shared spine (`references/`)** — the 9-dimension rubric library, the 5 foundation↔rubric pairs, `plugin-architecture.md`, `carve-method.md`, the authoring bridge, and the critic prompt corpus (`eval-prompts.md`), referenced by both skills via `${CLAUDE_PLUGIN_ROOT}`.
- **Bin** — `validate_plugin.py` (plugin + marketplace static validators, `selftest`, and the advisory `hook` mode), `check-foundations-coverage.py`, and `reference-lint.py` (fails on doc/command references that don't resolve on disk).

**Decision A (self-contained):** the four cross-cutting rubrics that score P1/P7/P8/P9 (`cold-start-orientation`, `skills-authoring`, `skill-extensibility`, `security-and-scope-containment`) are **co-located** from `skills-studio` rather than referenced across the install boundary — zero `../` cross-plugin paths, zero `dependencies`. (Future: promote the shared critics + cross-cutting rubrics to a foundation plugin both studios depend on, once skills-studio is also packaged.)

Status **draft, N=0 empirical applications** — every rubric dimension is a falsifiable hypothesis until applied to ≥3 real plugins. Build-time red-team (full 9-critic panel, given it bundles a hook and is a meta/orchestrator plugin) returned **CONDITIONAL**; all Critical/Major findings folded — every council agent tool-scoped to read-only (closing the P9 trifecta), `scripts/`→`bin/` drift and dead persona pointers fixed, hook invocation + injection-framing hardened, and a `reference-lint.py` + CI workflow added to gate the regression class. Structural items (council-calibration eval, dimension MECE audit) tracked in ROADMAP. See `reviews/2026-06-02-plugin-red-team.md`.
