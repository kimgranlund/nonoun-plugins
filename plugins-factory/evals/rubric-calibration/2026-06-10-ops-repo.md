# Rubric application — ops-repo (2026-06-10, cold)

**Candidate:** the maintainer's global `ops-repo` skill (repo-as-brain memory layer; v1.15.0, 44 references in 6 axes, 1 stdlib script, routing corpus, bundled 9-critic review). Already carved *conceptually* into agent-ops's `repo-ops` skill — scored here as the original, pre-carve. **Verdict: CONDITIONAL** — P1 4 · P2 3 · P3 4 · **P4 3** · P5 4 · P6 4 · **P7 3** · P8 4 · P9 4.

## The findings that set the verdict

- **P4 = 3 — two parent-tree reaches fail the install test:** SKILL.md cites `.docs/ops-family-shape.md` (lives *outside* the skill root, at the library level) and the CHANGELOG cites a library-level family gate script — both dangle the moment the directory is copied alone. Internal `../` cross-references all stay within the root (legal). Drift: the references INDEX self-describes as the "authoritative manifest" but carries a version header three majors stale.
- **P2 = 3 — primitive conversions a carve owes:** user-named recurring flows ("harvest", "import", the audit) are conversational prose by declared invariant — but in plugin form they are exactly the command primitive; the recommend-then-validate recipe prescribes two fresh-context agents *in prose* (the agent primitive); the Verify-Target gate depends on the model remembering to run the script (AP-P3-adjacent, mitigated by hard exit codes once invoked).
- **P7 = 3 — pinned by the no-command anchor** despite an above-bar routing surface (14 trigger + 6 adversarial phrases *with explicit `routes_to` hand-offs*).
- **Strengths:** P8 4 — exemplary changelog (behavior-impact annotations, review findings traced to closing versions) missing only a CI gate (self-acknowledged); P9 4 — the injection guard is *stated and labeled* (`[hypothesis]` with its structural backstop), secret-pattern redaction mechanized, harvest guard verified present.

## Top 3 carve fixes

1. Sever the parent-tree reaches (inline or co-locate the family-shape doc; drop the library-script citation); fix the stale INDEX header.
2. Add the missing primitives: thin commands for the named flows; promote recommend-then-validate to a real agent pair; optionally hook-enforce the apply-mode write gate.
3. Author the manifest from skill.json + `${CLAUDE_PLUGIN_ROOT}` wiring + the CI validate gate the candidate's own ROADMAP defers.

## Calibration notes

P4's install test discriminated best — a mechanical grep surfacing reaches a clean-on-disk read misses. P2 yielded three named primitive conversions rather than vibes. P8/P9 discriminated *positively* (anchors can reward, not just punish). P7's no-command anchor is near-tautological pre-carve — it measures the carve's to-do, not content quality; needs a pre-carve note. P6 mostly measured description verbosity (no MCP/siblings → expensive failure modes structurally absent).
