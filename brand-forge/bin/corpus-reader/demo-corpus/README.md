# Demo Corpus

A tiny, synthetic corpus committed beside the reader so a fresh clone renders something real. Every page is a few hundred bytes and exercises a feature — sections, status badges, provenance tags, cross-references, tables, highlighted code, mermaid, and the sanitizer probe. It doubles as the CI smoke fixture for the corpus pipeline (`build-sitemap.py` + `--init`).
