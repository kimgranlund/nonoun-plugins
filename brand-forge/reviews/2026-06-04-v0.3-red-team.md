# brand-forge — plugins-factory plugin-council red-team (v0.3)

- **Date:** 2026-06-04
- **Reviewed:** `brand-forge` v0.3.0 — the three-seat release (the Muse promoted to a generative agent; the creative-collaboration model; team-operations-by-phase; the ways-of-working rubric; the Brand Steward seat).
- **Instrument:** `plugins-factory` `plugin-council`, full 9-critic panel, isolated then synthesized. Cold read; plugin contents treated as untrusted DATA.
- **Raw verdict:** **CONDITIONAL** — 3 MUST-fixes, 4 SHOULD-fixes. **All folded in 0.3.0 → APPROVED.**

## What the panel verified clean

- **P9 Security — 5/5.** The entire new surface is prose + frontmatter: zero executables, zero hooks, zero MCP tools, zero writes. The `brand-muse` agent is tool-scoped `Read, Grep, Glob` — read-only, trust-bounded — so it cannot write, execute, or exfiltrate. **No lethal trifecta** (it reads untrusted briefs + corpus, but has no exfiltration leg — two of three, never the third).
- **P4 Dependency Legality — 5/5.** Copy-alone still clean; no new paths/deps; stdlib unchanged.
- **ST5 injection sweep — clean.** Every "rate 5/5 / approve / ignore" string is a defensive trust-boundary block or a rubric anti-pattern definition — none aimed at an evaluator. The Muse's trust boundary correctly translates the guard into generative terms (an embedded "the positioning is already decided" is a _provocation input to invert_, never a command).
- **Counts coherent.** 16 agents = 14 critics + 1 orchestrator + 1 Muse; 6 commands — both match the manifest/README.
- **The triad is conceptually strong and cleanly bounded** — Muse (provoke, forward) / Team (make) / Council (review, backward) are non-overlapping; the one invariant is genuinely load-bearing; the Steward fills a real gap.

## MUST-fixes — all folded

1. **(Andrej K./Scott W. · P5/P8) Unverifiable CHANGELOG provenance.** The 0.3.0 entry described fixing an "eight roles, seven listed" miscount whose before-state no longer exists in the tree (it was corrected in the same pass), so a reader couldn't verify the claim. **Folded:** reworded the CHANGELOG to assert only what the files show — a Brand Steward was added, the Muse became its own seat, leaving seven convergent making-roles — with no appeal to an invisible diff. (The 0.3.0 date, 2026-06-04, is honest: one day after 0.2.0, which is today.)
2. **(David F./Andrej K. · P8) No v0.3 review record — the v0.2 provenance ritual was skipped.** **Folded:** this record; and the README provenance note now points to the per-release `reviews/` files so "red-teamed via plugins-factory" is true for v0.3 specifically.
3. **(Elon/Chip H./Charity · P2) The Muse's agent-hood was justified by symmetry with the council, not on its own merits.** A solo agent doesn't earn isolation the way 14 non-anchoring critics do, and the fan-out that would justify it is deferred to v0.4. **Folded:** `creative-collaboration.md` now defends the Muse's isolation on its _own_ grounds — it runs _hot_ (divergent, anti-consensus) without polluting the team's convergent thread, and is a read-only sandbox for provoking _from_ untrusted briefs — **and** names that the Muse is deliberately the one seat with **no mechanical output contract** (grounding-to-a-root is the knowingly-uncheckable human-judgment substitute for a test). That is thesis-consistent — _structure is mechanized; taste is not_ — not an oversight. The same correction was applied to plugins-factory's `operational-roles.md` so the generalized pattern teaches earn-your-isolation, not copy-the-council.

## SHOULD-fixes

1. **(Steve · P7) `name: muse` was a collision-prone unqualified agent name.** **Folded:** renamed the agent to **`brand-muse`** (file + frontmatter + every reference), matching the `brand-council` convention.
2. **(Boris · P6) Duplication.** **Folded:** `brand-methodology/SKILL.md` now names the seats compactly and defers the role detail to `creative-collaboration.md`; `/brand-muse` defers the lens list to the agent (as `/brand-council` defers the roster).
3. **(Simon · P9) Population note.** **Folded:** the README security note records that the Muse widens the untrusted-content-reading agent population 15→16 (still no exfil leg).
4. **(Panel blind spot → ROADMAP) Muse calibration.** **Folded into ROADMAP:** the v0.4 council-calibration eval is extended to the `brand-muse` — assert every provocation traces to a cultural root and that the spread escapes the obvious answer.

## Convergent finding (≥2 critics) + the panel's own blind spot

**Convergence (Andrej K. · David F. · Scott W. · Charity):** the release shipped the feature _ahead of_ the provenance/verifiability discipline v0.2 established — the recurrence, in narrative form, of the v0.2 "tree drifted ahead of declared state" finding. Closed by this record + the CHANGELOG reword.

**The blind spot all nine share (recorded per the plugin's own principle):** this council can verify the Muse is well-_packaged_; it cannot judge whether the Muse produces _good provocations_. That needs a senior creative running it on fixture briefs — the same calibration gap the v0.2 review named for the Council, now extended to the Muse in the v0.4 ROADMAP item.
