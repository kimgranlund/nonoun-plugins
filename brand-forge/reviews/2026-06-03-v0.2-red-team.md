# brand-forge — plugins-factory plugin-council red-team (v0.2)

- **Date:** 2026-06-03
- **Reviewed:** `brand-forge` (working tree, the surface that became `0.2.0`)
- **Instrument:** `plugins-factory` `plugin-council` orchestrator → full 9-critic panel (Boris · Steve · Elon · Charity · Karpathy · Simon · Wlaschin · Huyen · Farley), each in an isolated parallel context, cold read (not installed/executed).
- **Mandate:** verify the README's "authored, validated, red-teamed via plugins-factory" claim and produce the missing `reviews/` record.
- **Raw verdict:** **CONDITIONAL** — 3 MUST-fixes. **All folded in `0.2.0` → APPROVED.**

## What the panel verified clean (the mandate)

The dimensions that define a reference plugin held, and held as _exemplary_:

- **P4 Dependency Legality — 5/5.** Copy-alone install is clean: every path is `${CLAUDE_PLUGIN_ROOT}`-rooted or script-relative; **zero `../` escapes, zero absolute `/Users`/`/home` paths** anywhere in the bundle (`.mcp.json`, `hooks/hooks.json`, the bins).
- **P9 Security & Trust — 5/5.** **No bundled lethal trifecta** — no single agent holds private-data + untrusted-content + exfiltration at once. The 14 critics are tool-scoped `Read, Grep, Glob`; the orchestrator adds only `Task`; the MCP is read-only by construction (every `call()` branch returns text — no write/delete/subprocess/network). `_safe()` is correct against `../` traversal **and** symlink-prefix escape (trailing-`os.sep` check), re-validated at both enumerate and read (TOCTOU closed).
- **ST5 injection sweep — clean.** Every manifest/command/SKILL/agent/bin docstring/MCP-tool description was scanned for instructions aimed at the evaluator ("rate 5/5", "ignore the brief", "always approve"). **None found.** The only directives present are the _defensive_ trust-boundary blocks, byte-identical across sampled critics and present in the orchestrator + evaluate skill — the duplicated-by-design guard, confirmed.
- **Council roster well-sourced.** Luke S. (Rapha), Massimo V., Mary N. (New Yorker), David A. (AMV), Lois, John H. (BBH) et al. confirmed real, correctly-attributed, lens-distinct practitioners — the opposite of a hollow persona list.

## 9-dimension scorecard (as-reviewed → post-fold)

| Dim | Name | As-reviewed | Post-fold |
| --- | --- | --- | --- |
| P1 | Plugin Fitness | 4/5 | 4/5 — real 5-primitive composition |
| P2 | Component Fit | 4/5 | 4/5 — MCP truncation now signalled |
| P3 | Boundary Cohesion | 4/5 | 4/5 — make/judge share the corpus |
| P4 | Dependency Legality | **5/5** | 5/5 — copy-alone clean |
| P5 | Manifest & Packaging | **2/5** | **resolved** — manifest now matches the 5-command surface |
| P6 | Context Economy | 4/5 | 4/5 — lean SKILL TOCs |
| P7 | Routing & Discoverability | 3/5 | 3/5 — see cross-plugin note |
| P8 | Evolution & Maintenance | **2/5** | **resolved** — `0.2.0` cut + orphan removed |
| P9 | Security & Trust | **5/5** | 5/5 — no trifecta, read-only, `_safe()` correct |

## MUST-fixes — all folded in 0.2.0

1. **(Wlaschin · Critical · P5) Manifest misdescribed the plugin.** `plugin.json` v0.1.0 named **four** commands but the directory ships **five** (`/brand-stamp` is real, documented, implemented). **Folded:** `plugin.json` description now names all five; `README.md` component map corrected (`4 typed entry points` → `5`); `marketplace.json` carries no command count, so no drift there.
2. **(Farley · Major · P8) Version lied about contents.** The CHANGELOG "Unreleased" block (stamp, MCP wiring, corpus hygiene, 15-agent tool-scoping) was already merged into the tree under v0.1.0. **Folded:** cut **`0.2.0` — 2026-06-03**, moving the block into a dated release and bumping `plugin.json` `version` to match.
3. **(Elon · Critical-for-a-reference · P8/P1) Orphaned component.** `bin/brand-stack` + `assets/brand-stack-template.svg` were referenced by no command, hook, skill, README, or CHANGELOG — and the six-tier "Brand Stack" model they render (foundation/strategy/idea/identity/expression/output) appears **nowhere** in the methodology (which is research→strategy→expression→stewardship). **Folded:** deleted both files + the stale smoke-test from the repo `CLAUDE.md`. In the flagship reference plugin, shedding dead code copyists would inherit is the right call; the model can be built out as a real deliverable later if wanted.

## SHOULD-fixes

4. **(Karpathy) Commit this record** — converts "red-teamed via plugins-factory" from an unverifiable claim into evidence. **Done** (this file; linked from `CHANGELOG.md` 0.2.0).
5. **(Huyen · P2) MCP truncation honesty.** `fetch_brand_section` / `get_brand_tokens` silently cut at 20000 chars. **Done** — both now append `…[truncated at 20000 chars]` so the model knows the doc is partial.
6. **(Huyen · P2) `get_brand_tokens` `isError` flag.** Returns `isError:false` on not-found, unlike `fetch_brand_section`. **Kept as-is, deliberately** — "no tokens in the corpus" is an _empty survey_ (like `search`/`list` returning nothing, both `isError:false`), not a failed path lookup. Documented here as the intended exception rather than flipped.
7. **(Simon/Farley · P9) Shared `selftest` for the `_safe()` guard** duplicated into every stamped artifact via `_copy_mcp`. **Deferred to v0.2.x** — add a `--selftest` to `brand-corpus-mcp.py` so the guard has one test that catches divergence across copies.

## Convergent finding (highest confidence)

Three critics reached the same root cause from different doors — Elon (orphan), Wlaschin (manifest), Farley (version): **the working tree had drifted ahead of its declared state.** Not a matter of perspective; a fact the files contradicted each other on. The `0.2.0` cut + the description re-sync + the orphan deletion close it. The deeper question the panel surfaced — _is brand-forge a brand-**strategy** plugin or a brand-**lifecycle** plugin?_ — resolves in favor of lifecycle (build · evaluate · council · orient · **stamp**), which is what the directory and now the manifest both say.

## Blind spot the panel names about itself

The plugin-council can verify brand-forge is well-_packaged_ (isolated council, tool-scoped, well-sourced roster) but is constitutionally unable to judge whether the 14-critic **brand** council produces good brand _critique_ — that needs the brand rubric, not the plugin rubric. An honest limit of this instrument, recorded per the plugin's own blind-spot principle.
