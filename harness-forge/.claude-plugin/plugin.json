{
  "name": "harness-forge",
  "version": "0.2.0",
  "description": "Hydrate a project to run looping, latticed agentic workflows. A kernel that scaffolds and operates a typed knowledge lattice — nine declarative layers × five scopes × a maturity state machine — driven by the engine (define→create→validate at the smallest signal-yielding scope, the signal minted from a real verifier command's exit status), the compass (scan→rank), and the regeneration loop (operate→ledger→distill→patterns). Ships an operating agent roster (a harness-builder orchestrator · harness-advancer · harness-auditor · harness-distiller), stdlib gates that mechanize typed naming, the dependency partial-order, lattice structural validation, and staleness propagation, a consent-gated installer (wire.py) that wires the blocking protected-verifier gate plus audit-trail and staleness-cascade hooks into the project's own worker loop, an advisory session hook that flags naming/staleness on save (never blocks), and a read-only lattice-query MCP over the project's .harness/ state. Self-contained.",
  "author": {
    "name": "Kim",
    "email": "kim.granlund@gmail.com"
  },
  "homepage": "https://github.com/kimgranlund/claude-plugins",
  "license": "MIT",
  "keywords": [
    "agentic",
    "harness",
    "lattice",
    "agent-loops",
    "orchestration",
    "evals",
    "verification",
    "autonomy",
    "ledger",
    "scaffold",
    "kernel",
    "meta"
  ],
  "userConfig": {
    "harness_dir": {
      "title": "Harness state directory",
      "type": "directory",
      "description": "Path to the project's `.harness/` directory (the durable lattice state — lattice.json, the layer dirs, signals/, ledger/) that the read-only lattice-query MCP reads. Optional — leave unset to run the engine, compass, gates, and agent roster without live retrieval.",
      "sensitive": false
    }
  }
}
