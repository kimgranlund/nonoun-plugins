# harness-forge — Roadmap

Open structural work, in rough priority. v0.4 is the kernel + the operating roster + the consent-wired loop + the **bounded autonomous loop** + the structural council, validated, selftested, and twice dogfood-red-teamed; the items below deepen it toward the full factory the foundations describe.

## v0.5 — deepen the loop + the second council's remaining deferrals

- ~~**`/harness-run` + the orchestrator's return + the wired stop-gate.**~~ **Shipped in 0.4.0** (per-cell breaker) **+ 0.4.1** (the GLOBAL caps enforced in code: `run-budget.py` + `gate-budget` deny every write once the run is exhausted — wall-clock/iterations/cells, computed from the deadline + ledger, no agent counter; `evals/global-bound/` proves it). Still open from this line: ~~a **token/dollar** budget meter~~ **— LANDED 0.5.15:** the run budget gained a `max_cost` axis — `run_budget_exhausted` sums the ledger's reported `cost.tokens` since `start_ts` against the ceiling, denied by the wired `gate-budget` in code (the orchestrator reports per-iteration cost; absent cost never trips it); `evals/global-bound/` proves it (1000-token cap + a 1200-token event → every write denied). Still open: **trust-trajectory automation** (auto-promote/demote a loop family by the measured false-pass rate — today the tier is read at the boundary, set by hand).
- **The discriminated-union for `blocked`** (Scott, v0.4.0 council) — replace the independent `blocked`/`blocked_reason` fields with one optional `block: {reason}` object so a reason-without-block is *unconstructable*, not just `check()`-flagged. Same move as the signal/maturity union below.
- ~~**The council as its own plugin (or a clean internal seam).**~~ **Decided 2026-06-16 — keep bundled (D-11).** Two critics (Steve Y. C1, Boris C. C1) flagged that the kernel-user carries the council's 8 always-on agent descriptions (1,727c ≈ **431 tok**, ~22% of the plugin's always-on tax) for a path most operating sessions never take. Re-examined: the agents read an *external* `.agents/harness/` by path, but the orchestrator drives through `bin/council-precheck.py` → `lattice.py` (the kernel read-path), so the council is **not cleanly separable** — under the zero-cross-plugin-dependency rule a split is *duplicate-and-sync* (vendor the kernel read-path behind a drift gate) for a ~431-token saving only in operate-only sessions. Cost stays **disclosed** (README "Honest scope"; `context-cost.py`). Revisit only if the council grows materially or a real kernel-without-judge user appears. (No native agent lazy-load exists — that's a host feature.)
- **Schema as the single source, not a second copy.** 0.3.1 made `check()` *read* the enums from `cell.schema.json` and catch unknown keys; **0.5.14 extended it to the SETTLED foothold-maturity set** (`x-settledMaturities`, drift-guarded) — the "requires `signal_refs`" maturities are now sourced from the schema, not a module-only constant. The deeper move still open (Scott W.): a discriminated union on `maturity` so a `validated` cell **without** non-empty `signal_refs` is *unrepresentable*, not merely caught. That needs a JSON-Schema validator the stdlib-only kernel lacks, so enforcement stays in `check()` for now — the schema is the single source of the *rule*, `check()` the enforcer.
- ~~**Post-deploy observability (Charity M.).**~~ **Shipped** — `/harness-status` (0.4.1) + the run-budget X/Y line, the unbounded alarm, and the gate-fire counter (0.4.2). Two remainders from this line. A **streaming** signal (`/harness-status` was a manual pull; a long unattended run emitted nothing to watch in real time — the PostToolUse hooks the spine of a tail-able progress line), and a **run-budget liveness marker** — *partially shipped (0.5.2/0.5.3)*: the **arming-gap** axis self-heals — a loop-active marker older than `LOOP_TTL_S` is treated as a crashed-run corpse, so `loop_unbudgeted`/`gate-budget` stop wedging the project (`/harness-status` + `run-budget.py stop` surface/clear it). **The global axis now self-heals too — LANDED 0.5.16:** `gate-budget` consults `lattice.run_dead(d, now)` when the budget is exhausted — no genuine loop activity within `LOOP_TTL_S` → a crashed/finished corpse → writes are freed (`/harness-status` shows `EXHAUSTED but DEAD`; `run-budget.py stop` clears it), while a LIVE loop over budget is still denied. The chosen liveness signal is **the loop's own ledger activity** (`validate`/`operate`/`advance` — the `hook:*` audit records, incl. a human's relevant write, are *excluded*), **not** a marker heartbeat: a human never emits an engine event and a runaway can't both skip validating and make progress, so the signal is unforgeable and the self-heal can't fail open (a heartbeat the agent had to refresh *would* fail open if forgotten). `evals/global-bound/` proves all three (live-over-budget denies · crashed corpse self-heals · a human's hook-record can't re-arm it). **Streaming — LANDED 0.5.17:** `/harness-status --stream [--follow]` renders the ledger (the real-time append stream the wired `emit-ledger` hook feeds) as a human-readable progress feed / live `tail -f` — a pure reader over existing state, no re-wire. **This observability line is now fully shipped.**
- **~~The arming step, mechanized (Chip/Andrej, v0.4.1 council)~~ — LANDED v0.5.0 (I-9).** The global bound was *enforced* in code but *armed* by the orchestrator's step 0; skip it and the run was unbounded, because the gate couldn't tell a loop-write from manual editing. **Closed by the loop-active marker:** `/harness-run` writes `.agents/harness/run/loop-active.json` (step 0a, `run-budget.py mark`) before arming the budget (step 0b, `run-budget.py start`), and `gate-budget` now denies every write while marked-but-un-budgeted — so forgetting step 0b fails closed. The marker is the loop-entry signal the gate lacked: it distinguishes the autonomous loop from a human, so manual editing and `/harness-advance` stay free; it's in the protected `.agents/harness/run/*` perimeter (a worker can't clear it); `/harness-status` shows the state; `evals/global-bound/` proves the closure with no model agent. The residual shrank from "forget step 0" to "skip the whole preamble."
- ~~**Consent as a mechanism, not prose (Simon W.).**~~ **LANDED 0.5.18.** `wire.py apply` now refuses to mutate the project's `.claude/settings.json` without an explicit `--confirm` (exit 3, echoing the absolute target + pointing at `wire.py plan`) — consent is a required act, not a slip-skippable instruction in `harness-seed.md`. The `apply()` function stays callable in-process (the gate is in the CLI dispatcher); the eval callers + `/harness-seed` + RUN.md pass `--confirm`; selftest covers it.
- ~~**The seed-into-loop gate installer.**~~ **Shipped in 0.2.0** — `bin/wire.py` (plan/apply/check/unwire, consent-gated, idempotent, self-protecting, version-stamped) + `/harness-seed` step 4.
- ~~**`emit-ledger` + `propagate-staleness` as project hooks.**~~ **Shipped in 0.2.0** — both gateverb species, selftested, wired by the installer.
- ~~**A real first-slice walkthrough.**~~ **Shipped in 0.2.1** — `evals/first-slice-walkthrough/replay.py` (CI) + `RUN.md`; it caught the seed's circular-verifier deadlock at authoring time. Still open: stamp default `budget` fields at seed (the positive-control baseline's one real weakness, H5).

## v0.2 — calibration (the catalog standard)

- ~~**Council-calibration fixtures.**~~ **Shipped in 0.2.1** — `evals/calibration/`: four planted-defect fixtures + `check_fixtures.py` (the gates catch each defect; a clean control passes) + five judge baselines in `runs/` gated by `check_baselines.py` (concept recall, incl. the injection-quoted-not-obeyed assertion).
- ~~**`bin/` behavioral gates in CI.**~~ **Shipped in 0.2.1** — the planted partial-order violation is `fixtures/rubric-before-spec/` (caught by `lattice.py check`); the failing-verifier-does-not-advance case is in `validate.py selftest`; the walkthrough is the end-to-end positive.

## v0.3 — the structural-critic council

- ~~**A harness critic council.**~~ **Shipped in 0.3.0** — the 7 structural critics + the `harness-council` orchestrator + `/harness-council`, with a live calibration run recorded against the unearned-autonomy fixture. Still open: convene the council against the remaining three fixtures (one live baseline exists; N=4 is the full set), and a `/harness-council` lens-subset smoke in CI.

## v0.3+ — the kernel / kit / instance tiers

- **Family kits.** The foundations name three tiers — invariant machinery (kernel), family bindings (kit), accreted project state (instance). v0.1 is the kernel. Ship **kits**: a `harness-forge-kit-*` family binding (seed corpora, family rubrics, harness adapters) for a concrete domain (a research loop, a coding loop, a docs loop), imported as precedent and marked as such.
- **Harness adapters.** `bin/validate.py` (v0.1.1) already runs a cell's verifier *command* and mints the signal from its exit status — the runner exists. Add pluggable, named verifier adapters (pytest, link-check, screenshot-diff, llm-judge) bound to rubric cells by configuration, so a cell's `validate` step selects a real check by name, not by the operator typing the command each time.
- **A `lattice render` view.** A derived, human-readable lattice grid (layers × scopes, color by maturity) — a status surface over `lattice.json`, the canonical state staying the JSON.

## Known limitations (v0.2)

- The blocking gates are installed **only by consent** (`wire.py apply`, offered by `/harness-seed`) — an unwired project's protection is the worker's narrow tool scope plus discipline, and `wire.py check` says which state you're in. A worker holding `Bash` can still route around any path-glob gate; keep workers Bash-less.
- The wired hook copies in `.agents/harness/hooks/` refresh on re-`apply`, not automatically on plugin update (`wire.py check` WARNs on drift).
- The probe-cost ranking falls back to a constant until the ledger accrues history (by design — measured, not estimated).
- No family kits yet — the kernel is domain-agnostic; cold-start precedent must be authored per project until kits land.
