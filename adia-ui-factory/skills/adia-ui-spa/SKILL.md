---
name: adia-ui-spa
description: >
  Architect and author an adia-ui app in SPA mode — static host document, the four-axis structure, content-less <router-ui> routing, the client DataClient/projection data-flow, single-owner state. Use when the app is a client-rendered SPA (Vite / vanilla).
---

# adia-ui-spa

> **Stub — scaffolded in phase (a); content authored in phase (b).**

This skill will own:

- the static host document + cascade-ordered links + one registration script
- the four-axis structure (spec/plan/app/skills) + page-trio
- content-less <router-ui> routing + single-owner state
- the DataClient → ProjectionMapper data-flow + attribution

Source (to vendor/synthesize): the `app-authoring-best-practices` methodology (SPA) and `adia-ui-kit/rendering-model` (SSR) from the framework, plus the a2ui MCP tool surface.
