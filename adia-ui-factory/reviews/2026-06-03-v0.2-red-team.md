# adia-ui-factory v0.2.0 — plugins-factory 9-critic red-team (2026-06-03)

Ran the full council cold against the v0.2 surface (11 skills · 16 references · 7 commands · 2 bins · 1 hook · the wired+pinned a2ui MCP · no agents), in parallel isolated contexts, then synthesized.

## Verdict: CONDITIONAL → fixes folded → **APPROVED** (0.2.0 cut)

**No runtime Critical.** The two "Critical" labels (Wlaschin, Farley) were the same defect — release-coherence drift, not a runtime break. What kept it sub-Critical everywhere else: the trust-boundary guard is now universal across all 11 skills (Simon verified, incl. the generated-A2UI render surface, which also has a validate-before-render gate); the MCP is version-pinned; the hook is provably read-only + never-blocks (self-tested); there are no agents, so no bundled lethal trifecta.

## Convergent findings → resolution

| # | Finding | Critics | Resolution |
| --- | --- | --- | --- |
| 1 | **Version/README drift** — plugin.json 0.1.0 vs skills 0.2.0 vs CHANGELOG "in progress"; README documented 6 skills | 8/9 (Wlaschin, Farley **Critical**) | Cut 0.2.0: bumped manifest, added spa/ssr `version`, dated the changelog, rewrote README to 11 skills |
| 2 | **Commands lag skills** — dangling "Step 0"; `/adia-scaffold` & `/adia-wire` routed around project/data; 6 command-less | Steve, Charity, Boris, Huyen | Fixed the dangling ref; repointed scaffold→project, wire→data; added `/adia-migrate` + `/adia-genui`; documented the curated spine |
| 3 | **`[gate]` overstatement** — ~35 gates self-asserted under the same badge as ~9 mechanized; `audit:*` falsely called "the mechanized layer" | Karpathy (verifiability 2.5/5), Charity (P2 2/5) | Corrected the `audit:*` claim (app-repo-side, not shipped); marked render / validate-before-render / a11y as **self-verified**, distinct from the `adia-lint`-mechanized gates |
| 4 | **MCP ledger gap** — `refine_ui` driven but absent from `a2ui-mcp-tools.md`; "~24" unverified | Huyen (Major) | Added `refine_ui`; count left explicitly approximate (a `tools/list` snapshot tracked for later) |
| 5 | **Scaffolder over-claim** — bin doesn't one-shot rollup/shared-foundation | Elon (High) | project skill now states the bin covers single-surface + page + component; rollup/shared are composed per `project-shapes.md` |
| 6 | **Unguarded shell-ref links** (reference-lint skipped bare tokens) | Farley (Low) | Per-shell refs given resolvable `${CLAUDE_PLUGIN_ROOT}` paths |

## 9-dimension scorecard (pre → post-fold)

| Dim | Pre | Post | Note |
| --- | --- | --- | --- |
| P1 fitness | 3–4 | ~4 | README/version fixed; the SPA/SSR fork is a real axis |
| P2 component-fit | 2–4 | ~4 | Huyen: zero miscarved primitives; gate honesty folded |
| P3 boundary | 4 | 4 | one job, real internal granularity |
| P4 deps/blast | 4–5 | ~4 | MCP pinned + disclosed + disable path |
| P5 manifest | 2 | ~4 | version drift fixed |
| P6 context-economy | 4 | 4 | descriptions tight; MCP cost disclosed — **accepted trade** |
| P7 routing | 3 | ~4 | commands repointed/added |
| P8 evolution | 2–5 | ~4 | dated changelog + version coherence |
| P9 security | 4 | 4 | universal trust boundary; pinned MCP; never-block hook |
| (verifiability) | 2.5 | ↑ | `audit:*` claim fixed; gates marked mechanized-vs-self-verified |

## Accepted / deferred (ROADMAP "Open")

- **11 skills, not 8** (Elon vs Steve/Boris/Huyen) — the signed-off carve; genui↔llm and migrate↔verify merges weighed and declined (clean boundaries; comprehensiveness was the chosen trade).
- **P6 always-on MCP** — pinned + disclosed + disable path; per-tool scoping is an upstream/host limit, not fixable from `.mcp.json`.
- A committed `tools/list` snapshot for the pinned MCP (makes the ~24 count auditable); a `bin/adia-probe` to mechanize the browser-render gate; uniform snapshot banners on every concrete-API reference.

Method: `references/critics/eval-prompts.md` (PF/CF/BC/DL/MP/CE/RD/EV/ST + S-series). The artifact was treated as untrusted data; ST5 scan clean.
