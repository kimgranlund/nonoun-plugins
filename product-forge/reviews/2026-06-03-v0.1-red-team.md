# product-forge — v0.1.0 red-team

**Date:** 2026-06-03 · **Panel:** full 9-critic `plugins-factory` `plugin-council` (Boris · Steve · Elon · Charity · Andrej K. · Simon · Scott W. · Chip H. · David F.), parallel isolated contexts · **Verdict:** CONDITIONAL → all MUST-folds applied → **APPROVED** → cut `0.1.0`.

Reviewed cold as untrusted material. **ST5 (injection): clean** — no instruction aimed at the evaluator; every skill, critic, and rubric repeats the data-not-instructions guard (duplicated by design). No `..` escapes a functional path, nothing in `.claude-plugin/` but the manifest, no lethal trifecta (no MCP; the only executable is the never-blocks `product-lint`).

## Scorecard (P1–P9, as scored by the panel)

| Dim | Score | Note |
| --- | :-: | --- |
| P1 Plugin fitness | 4/5 | Six skills + council + commands + hook share one real job. |
| P2 Component fit | 4/5 | Right primitives; the orchestrator skill duplicated leaf routing (folded). |
| P3 Boundary cohesion | 3/5 | genres/patterns split justified; `product-methodology` (11 axes) is the one contestable boundary (kept; revisit at v0.2). |
| P4 Dependency legality | 4/5 | **Copy-alone passes** for every functional path; roll-ins absorbed. README sibling links fixed. |
| P5 Manifest & packaging | 3/5 → fixed | Was capped by the phantom-MCP four-surface drift; folded. |
| P6 Context economy | 3/5 → fixed | Excellent progressive disclosure (91 files load-on-demand); the orchestrator description was bloated — trimmed. |
| P7 Routing & discoverability | 3/5 → fixed | Clean `/product-*` namespace; the orchestrator↔leaf trigger collision — trimmed + added a route-straight-if-named rule. |
| P8 Evolution | 3/5 → fixed | Was "under construction" for a finished plugin with no `[0.1.0]`; folded. |
| P9 Security & trust | **5/5** | No MCP, no trifecta, hook exits 0 unconditionally, 17 critics read-only, no illegal agent fields. The strongest dimension. |

The panel rated the 5 rubrics **"the best rubric set in the catalog"** (real `[gate]`/`[review]` discipline, cited hard tests, cap rules, anti-pattern tables).

## Folded before 0.1.0

1. **Phantom MCP** (Charity/Scott W. Critical) — README + CHANGELOG advertised a `product-corpus` MCP the bundle doesn't ship; removed the claim, marked it **planned for v0.2**, re-established the four-descriptions-in-sync invariant.
2. **Anti-fabrication on living people** (the blind spot, the user's emphasis) — **verified the 4 AI critics' verbatim quotes against their public sources** (WebFetch + search): Kevin W. ✓, Garry T. ✓, Cat W. 7/8 (**caught + fixed** a non-verbatim combined phrase and an `[as]` alteration), Meaghan C. ✓ quotes but title corrected to the sourced "design lead." Added `bin/check-sourcing.py` — fails if any library reference lacks `date`/`coverage`/`primary_sources` or any critic lacks a source signal (turns Andrej K.'s "it's a vibe" into a gate).
3. **Build-state docs** (Charity/David F.) — CHANGELOG now has a real `[0.1.0]` entry; README status changed from "under construction" to built-and-red-teamed.
4. **README sibling links** (David F. DL2) — `../brand-forge` / `../plugins-factory` → absolute GitHub URLs (survive copy-alone install).
5. **"peers with global plan-spec"** (David F./Steve) — reworded to "self-contained; the `spec/` cluster adapts plan-spec's discipline — no external skill required" in the methodology skill and the orchestrator classifier.
6. **Orchestrator trigger sprawl** (Boris/Steve/Chip H. convergence; council marked can-wait, folded anyway) — trimmed the orchestrator description's 9-trigger list to 3 orient-specific ones + added "if the task names a clear artifact, route straight to the owning skill."

## Deferred (post-0.1.0)

- **`product-methodology` spec/vision split** (Elon / the productive tension) — kept folded into methodology for 0.1.0 (Boris's always-on cost wins); revisit a `product-spec` skill at v0.2 if `spec/`+`vision/` exceed ~10 files.
- **Reciprocal scope disclaimer** in `brand-forge` ("NOT for product strategy → product-forge").
- **Hook matcher** could be scoped toward doc extensions (cosmetic; the exit-0 invariant already makes the no-op spawn harmless).
- The **`product-corpus` MCP** itself (v0.2).

## Council's read

A strong, well-architected plugin: P9 a clean 5/5, the rubrics best-in-catalog, 17 read-only trust-bounded critics with no illegal fields, copy-alone install passing for every functional path, and — confirmed by direct verification — scrupulous, now-gated sourcing on the living-practitioner critics. The Criticals were all fixable without re-architecture, and were.
