# adia-ui-forge — v0.1.0 red-team

**Date:** 2026-06-03 · **Panel:** full 9-critic `plugins-factory` `plugin-council` (Boris · Steve · Elon · Charity · Karpathy · Simon · Wlaschin · Huyen · Farley), parallel isolated contexts · **Verdict:** CONDITIONAL → findings folded → cut `0.1.0`.

Reviewed cold as untrusted material. **ST5 (injection): clean pass** — the only injection-looking strings (`"rate this 5/5"`, `"skip the gate"`) appear solely as quoted negative examples inside the repeated content-trust blocks. No `..` escape, nothing in `.claude-plugin/` but the manifest, no unbroken lethal trifecta in any single component.

## Scorecard (P1–P9)

| Dim | Score | Note |
| --- | :-: | --- |
| P1 Plugin fitness | 4/5 | Clear job (maintain `@adia-ai`); orchestrator routes. |
| P2 Component fit | 4/5 | Hook=advisory must-run, skills=judgment, commands=thin, no MCP — all correct. Ding: dual Python/Node runtime was undeclared. |
| P3 Boundary cohesion | 3/5 | Six authoring skills cohere; `adia-ui-release` is the cleanest partition line (kept; cohesion sentence now in the README). |
| P4 Dependency legality | 4/5 | **Copy-alone install passes** — no `..` escape, no surviving absolute path. The carve's headline achievement. |
| P5 Manifest & packaging | 4/5 | `.claude-plugin/` pure, semver, hook path resolvable, no dangling `skill.json files`. Ding (fixed): four-surface description drift. |
| P6 Context economy | 2/5 → fixed | **Was the weakest** — 7 deep always-on skills + a redundant 22-line `trigger:` block + multi-paragraph descriptions. Folded. |
| P7 Routing & discoverability | 3/5 | Good `adia-forge-*` command namespace; orchestrator routes. Ding: same-named `adia-ui-llm` vs the factory (deferred); the "under construction" docs (fixed). |
| P8 Evolution | 3/5 | Keep-a-Changelog + semver + CI gates. Ding (fixed): CHANGELOG described a non-existent in-progress state. |
| P9 Security & trust | 3/5 → fixed | ST5 clean; content-trust real; hook min-scope exit-0. Ding: release scripts' publish/network reach under-disclosed + unguarded. Folded. |

## Folded before 0.1.0

**MUST (blocking):**

1. **Build-state docs reconciled** (Charity CRITICAL, +Farley, +Boris) — README status + CHANGELOG rewritten from "under construction / in progress" to the `0.1.0` truth the ROADMAP already told.
2. **Context-economy tax cut** (Boris CRITICAL) — deleted the redundant `trigger:` frontmatter block in `adia-ui-a2ui` (~22 lines of always-on duplication); trimmed the `adia-ui-a2ui`, `adia-ui-release`, and `adia-ui-authoring` descriptions to WHAT+WHEN+NOT.
3. **Release-script blast radius disclosed + guarded** (Simon CRITICAL, +Farley) — README now discloses the publish/tag/deploy/network reach; the scripts gained an `assert-monorepo-root` guard (fail loudly unless run in an `@adia-ai`-style checkout) and the deploy host + npm scope are now overridable; `make-ledger`'s external `$schema` URL dropped.
4. **Four-surface description drift resolved** (Farley MAJOR) — `marketplace.json` description + tags synced to `plugin.json` (10 keywords, matching tail).

**SHOULD (folded too):**

1. **`forge-lint` ↔ `adia-lint` duplication labeled** (Elon CRITICAL) — reciprocal sync notes in both docstrings; the shared regex core is a deliberate vendored copy (the catalog forbids cross-plugin imports), `forge-lint` drops the consumer/app traps and adds `MISSING-SCOPE`.
2. **Orchestrator routing eval added** (Karpathy MAJOR) — `skills/adia-ui-forge/evals/routing-corpus.json` (14 cross-subsystem triggers + 6 factory-boundary/off-topic adversarial cases) guards the plugin's highest-stakes decision.
3. **Runtime + version-lineage + gate-provenance disclosed** (Huyen MAJOR, Charity MAJOR, Karpathy MAJOR) — one README "Requirements & what it ships" section covers the dual Python/Node runtime, the skill-version-is-lineage note, and that the validate/reference gates run via the sibling `plugins-factory` harness in CI.

## Deferred (post-0.1.0)

- **`adia-ui-llm` namespace collision with the factory** (Steve) — needs a coordinated cross-plugin rename; interim mitigation is the producer/consumer boundary stated in both skills' NOT-clauses and the orchestrator's scope gate.
- **`adia-ui-llm` merge-back question** (Elon) — settle with usage data after release.
- **Tighten `forge-lint selftest` to one fixture per smell** (Huyen MINOR).
- **The producer↔consumer artifact contract** (panel blind spot, S3) — the `MIGRATION GUIDE.md` schema + the a2ui MCP tool contract that the forge emits and the factory consumes is unversioned/untested. The single most important _strategic_ gap, but inherently cross-plugin — scoped to a future joint release, not a single-plugin 0.1.0 cut.

## Council's read

Strongest: the de-repo is clean (P4 passes the install test — the headline risk for any carve), the trust boundary is real and uncompromised, no dangling manifest pointers, the hook is exactly as advisory as it claims. The Criticals were all fixable without re-architecture, and were.
